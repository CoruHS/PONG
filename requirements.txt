# =========================
# RL Pong stack (Python 3.12)
# =========================

# ---- PyTorch (choose ONE of these) ----
# CUDA 13.0 RC build (requires NVIDIA + CUDA 13 driver/runtime)
--extra-index-url https://download.pytorch.org/whl/test/cu130
# torch==2.9.0

# CPU-only (comment the two lines above, and use this line instead):
torch==2.9.0

# ---- RL + Multi-agent Atari ----
stable-baselines3==2.4.0
gymnasium==1.0.0
pettingzoo[atari]==1.25.0
supersuit==3.9.3

# Optional single-agent ALE (harmless to include; useful for ALE envs/tools)
ale-py==0.11.0

# ---- Core libs & UI ----
# Numpy kept <2.4 to avoid edge-case wheels; plays well with torch 2.9
numpy>=1.26,<2.4
opencv-python>=4.8
pygame==2.6.1

# ---- ROM installer (non-interactive license) ----
autorom[accept-rom-license]>=0.6.1

# ---- Logging ----
tensorboard>=2.13
